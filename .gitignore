.env
node_modules/
__pycache__/
*.py[oc]
.venv/
dist/
logs/
.agents/
# Local agent state (session plans, skills) and machine-specific compose
# overrides — per-developer, never deployed.
.zcode/
docker-compose.override.yml